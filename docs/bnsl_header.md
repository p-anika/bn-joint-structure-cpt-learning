Required file header for successful GOBNILP parsing


A B C D E F G H     ← variable names FIRST
SCORES START         ← then this
8                    ← THEN the variable count
0 10                 ← then the data blocks begin